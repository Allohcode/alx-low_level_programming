#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */

int main(void)
{
	char c;

	c = 'A';

	if (c >= 'A' && c <= 'Z')
	{

		return (1);
	}
	else
	{
		return (0);
	}

}
