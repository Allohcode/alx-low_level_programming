#include <stdio.h>
#include <stdlib.h>
/**
 *main -E entry point
 *while for loop
 *Return: Always 0 (success)
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
		i = i + 1;
	}
		return (0);
}
