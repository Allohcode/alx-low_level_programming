#include <stdio.h>
#include <stdlib.h>
/**
 *main -E entry point
 *while for loop
 *Return: Always 0 (success)
 */

int main(void)
{
	char i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
		return (0);
}
