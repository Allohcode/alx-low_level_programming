#include <stdio.h>
#include <ctype.h>
/**
 * main -E entry point
 * char for alphabets
 * int for loop
 * Return: Always 0 (success)
 */

int main(void)
{
	char i;

	for (i = "a"; i < "z"; i++)
	{
		putchar(i);
		
	}
	for (i = "A"; I < "Z"; i++)
	{
		putchar(i);
	}
		putchar('\n');
		return (0);
}
