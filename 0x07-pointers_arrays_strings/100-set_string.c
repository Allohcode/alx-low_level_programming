/**
 * set_string - function that sets the value of a pointerto a char
 * @s: pointer to the address of string s
 * @to: string to set the valu to
 *
 * Return: nothig
 */

void set_string(char **s, char *to)
{
	*s = to;
}
