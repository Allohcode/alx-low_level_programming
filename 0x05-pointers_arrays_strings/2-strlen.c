#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - checks the length of a string
 * @s: inpout to function _strlen
 * Return: Alwaus 0 (success)
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);

}
