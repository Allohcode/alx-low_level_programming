#include <stdlib.h>
#include "main.h"

/**
 * malloc_checked - function to allocate memory usingmalloc
 * @b: number of byte to allocate
 *
 * Return: a pointer to allocate memory
 */

void *malloc_checked(unsigned int b)
{
	void *ptr;

	if (ptr == NULL)
	exit(98);

	return (ptr);
}
